import React from "react";
import { LedgerCalc, LedgerLog } from "../components";

const Ledger = () => {
  return (
    <div>
      <LedgerCalc />
      <LedgerLog />
    </div>
  );
};

export default Ledger;
